import Button from '@material-ui/core/Button';

export const SubmitButon = () => {
  return (
    <Button
      variant="contained"
      color="primary"
      type="submit"
      style={{ width: '100%' }}
    >
      チャットをはじめる
    </Button>
  );
};
