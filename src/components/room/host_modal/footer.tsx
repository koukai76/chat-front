const footer = () => {
  return (
    <button
      type="button"
      className="btn btn-secondary"
      data-bs-dismiss="modal"
      id="_close"
    >
      閉じる
    </button>
  );
};

export default footer;
